 
/*全自动制样机监控界面*/
 
 var AutoMakerV8Cef;
    if (!AutoMakerV8Cef)AutoMakerV8Cef = {};

    (function() {
      
      //查看故障信息
      AutoMakerV8Cef.GetHitchs=function(paramSampler){
      native function GetHitchs(paramSampler);
      return GetHitchs(paramSampler);
      };

      
      //查看故障信息
      AutoMakerV8Cef.presam=function(paramSampler){
      native function presam(paramSampler);
      return presam(paramSampler);
      };

      
      //查看故障信息
      AutoMakerV8Cef.nextsam=function(paramSampler){
      native function nextsam(paramSampler);
      return nextsam(paramSampler);
      };

       //查看故障信息
      AutoMakerV8Cef.OpenZXQSRecord=function(){
      native function OpenZXQSRecord();
      OpenZXQSRecord();
      };

    })(); 